{% extends 'base.html.twig' %}

{% block body %}
    <h1>Coordonnee</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coordonnee.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ coordonnee.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ coordonnee.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ coordonnee.email }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ coordonnee.adresse }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ coordonnee.telephone }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('prefix_new_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('prefix_new_edit', { 'id': coordonnee.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
